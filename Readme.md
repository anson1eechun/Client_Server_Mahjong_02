# Client-Server Mahjong 執行指南 (Web 版)

本專案已升級為 **Web 架構**。

## 🚀 如何執行

請依照順序執行：

### 第一步：啟動 WebSocket 伺服器

在終端機執行：

```bash
killall java  # 先清空舊程序確保端口乾淨
mvn exec:java -Dexec.mainClass="com.mahjong.server.MahjongWebSocketServer"
```

**成功徵兆：** 看到 `Mahjong WebSocket Server started on port: 8888`。

### 第二步：開啟網頁客戶端

1.  開啟 Finder，前往專案資料夾：
    `src/main/resources/web/`
2.  雙擊 **`index.html`** (會用 Chrome 或 Safari 開啟)。
3.  輸入暱稱，點擊 Start Game。

---

## 遊戲操作
*   **出牌**：直接點擊畫面下方的麻將牌。

## 測試與報告
*   **單元測試**：核心邏輯未變動，依然可使用 `mvn test` 驗證。