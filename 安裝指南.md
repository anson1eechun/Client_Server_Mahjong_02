# 環境安裝指南

## ⚠️ 當前環境問題

檢測到以下問題：
1. **Java 版本**：當前為 Java 8，專案需要 **Java 17 或以上**
2. **Maven**：未安裝或未配置到 PATH 環境變數

## 📋 安裝步驟

### 步驟 1：安裝 Java 17

#### 方法 A：使用 Oracle JDK
1. 前往 [Oracle JDK 17 下載頁面](https://www.oracle.com/java/technologies/javase/jdk17-archive-downloads.html)
2. 下載 Windows x64 安裝程式
3. 執行安裝程式並完成安裝
4. 配置環境變數 `JAVA_HOME` 指向 JDK 17 安裝目錄

#### 方法 B：使用 OpenJDK（推薦）
1. 前往 [Adoptium (Eclipse Temurin)](https://adoptium.net/zh-CN/temurin/releases/?version=17)
2. 下載 Windows x64 JDK 17
3. 解壓縮到 `C:\Program Files\Java\jdk-17`
4. 配置環境變數：
   - `JAVA_HOME` = `C:\Program Files\Java\jdk-17`
   - 將 `%JAVA_HOME%\bin` 加入 `Path` 環境變數

#### 驗證安裝
```powershell
java -version
# 應該顯示 java version "17.x.x"
```

### 步驟 2：安裝 Maven

1. 前往 [Apache Maven 下載頁面](https://maven.apache.org/download.cgi)
2. 下載 `apache-maven-3.9.x-bin.zip`
3. 解壓縮到 `C:\Program Files\Apache\maven`
4. 配置環境變數：
   - `MAVEN_HOME` = `C:\Program Files\Apache\maven`
   - 將 `%MAVEN_HOME%\bin` 加入 `Path` 環境變數

#### 驗證安裝
```powershell
mvn -version
# 應該顯示 Apache Maven 3.9.x
```

### 步驟 3：安裝專案依賴

在專案根目錄執行：

```powershell
# 清理並編譯專案（會自動下載依賴）
mvn clean compile

# 或者只下載依賴
mvn dependency:resolve
```

### 步驟 4：啟動應用程式

#### 方法 A：使用 PowerShell 腳本（推薦）
```powershell
.\start_demo.ps1
```

#### 方法 B：手動啟動
```powershell
# 終端 1：啟動伺服器
mvn exec:java -Dexec.mainClass="com.mahjong.server.MahjongWebSocketServer"

# 終端 2：開啟瀏覽器（重複 4 次）
Start-Process "src\main\resources\web\index.html"
```

## 🔧 快速安裝腳本（可選）

如果您有 Chocolatey 套件管理器，可以使用以下命令快速安裝：

```powershell
# 安裝 Chocolatey（如果尚未安裝）
Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))

# 安裝 Java 17
choco install openjdk17 -y

# 安裝 Maven
choco install maven -y

# 重新啟動 PowerShell 以載入環境變數
```

## 📝 注意事項

1. **Java 版本**：專案必須使用 Java 17，Java 8 無法編譯
2. **環境變數**：安裝後需要重新啟動 PowerShell 或命令提示字元才能生效
3. **端口占用**：如果 8888 端口被占用，需要先關閉占用該端口的程序

## 🆘 疑難排解

### 問題：Maven 命令找不到
**解決方法**：
1. 確認 Maven 已正確安裝
2. 確認 `%MAVEN_HOME%\bin` 已加入 PATH
3. 重新啟動 PowerShell

### 問題：Java 版本錯誤
**解決方法**：
1. 確認已安裝 Java 17
2. 確認 `JAVA_HOME` 指向 Java 17
3. 執行 `java -version` 確認版本

### 問題：端口被占用
**解決方法**：
```powershell
# 查看占用 8888 端口的程序
netstat -ano | findstr :8888

# 終止該程序（將 PID 替換為實際的進程 ID）
taskkill /PID <PID> /F
```

